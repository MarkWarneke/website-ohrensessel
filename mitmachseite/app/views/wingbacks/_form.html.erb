<%= form_for(@wingback) do |f| %>
  <% if @wingback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wingback.errors.count, "error") %> prohibited this wingback from being saved:</h2>

      <ul>
      <% @wingback.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
